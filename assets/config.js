/**
 * Covenant Eyes Router – Static config
 * Set WEBHOOK_URL to receive leads server-side (e.g. Zapier, Make, custom endpoint).
 * Leave empty string to only store leads in localStorage.
 */
var WEBHOOK_URL = "";
